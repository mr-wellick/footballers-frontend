import { MirrorPoolsIcon } from '../src/icons';

const Login = () => {
  return (
    <section>
      welcome to the login page
      <MirrorPoolsIcon />
    </section>
  );
};

export default Login;
